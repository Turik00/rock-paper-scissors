This folder contains shared logic with the backend.
It's located here and not outside the client folder as CRA webpack does not allow references outside root folder.
The other 'less dirty' way will be to eject CRA build process.